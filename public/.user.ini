; Optional: per-directory PHP overrides for hosts that support .user.ini
; (php-fpm / CGI). Ignored on servers that don't — edit the real php.ini instead.
upload_max_filesize = 4G
post_max_size = 4G
max_execution_time = 600
max_input_time = 600
memory_limit = 512M
